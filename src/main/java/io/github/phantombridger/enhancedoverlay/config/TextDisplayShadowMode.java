package io.github.phantombridger.enhancedoverlay.config;

public enum TextDisplayShadowMode {
    // I know that enums are usually ALL_CAPS but this made it easier to use them in the config
    Enable,
    Disable,
    Default
}